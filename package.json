{
  "name": "ts-node-base-project",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "dev": "tsnd --respawn -r tsconfig-paths/register --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "keywords": [],
  "author": "Suleware",
  "license": "ISC",
  "description": "base project for further developments",
  "devDependencies": {
    "@types/node": "^22.5.0",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.5.4"
  }
}
